# This Software (Dioptra) is being made available as a public service by the
# National Institute of Standards and Technology (NIST), an Agency of the United
# States Department of Commerce. This software was developed in part by employees of
# NIST and in part by NIST contractors. Copyright in portions of this software that
# were developed by NIST contractors has been licensed or assigned to NIST. Pursuant
# to Title 17 United States Code Section 105, works of NIST employees are not
# subject to copyright protection in the United States. However, NIST may hold
# international copyright in software created by its employees and domestic
# copyright (or licensing rights) in portions of software that were assigned or
# licensed to NIST. To the extent that NIST holds copyright in this software, it is
# being made available under the Creative Commons Attribution 4.0 International
# license (CC BY 4.0). The disclaimers of the CC BY 4.0 license apply to all parts
# of the software developed or licensed by NIST.
#
# ACCESS THE FULL CC BY 4.0 LICENSE HERE:
# https://creativecommons.org/licenses/by/4.0/legalcode
variables:
  RESTAPI_IMAGE: restapi

build:restapi:push:
  stage: build:image:lab
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  before_script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"${ARTIFACTORY_REPO}\":{\"auth\":\"${ARTIFACTORY_AUTH}\",\"email\":\"${ARTIFACTORY_EMAIL}\"}}}" > /kaniko/.docker/config.json
    - wget -qO - http://pki.mitre.org/MITRE%20BA%20ROOT.crt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
    - wget -qO - http://pki.mitre.org/MITRE%20BA%20NPE%20CA-3%281%29.crt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
    - wget -qO - http://pki.mitre.org/MITRE-chain.txt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
  script:
    - >-
      /kaniko/executor
      --cache=true
      --cache-repo=${ARTIFACTORY_UNTRUSTED_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}/cache
      --registry-mirror ${ARTIFACTORY_REPO}
      --context ${CI_PROJECT_DIR}
      --dockerfile ${CI_PROJECT_DIR}/docker/${RESTAPI_IMAGE}/Dockerfile
      --destination ${ARTIFACTORY_UNTRUSTED_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}:${CI_COMMIT_SHA}
      --build-arg MINICONDA_VERSION=${MINICONDA_VERSION}
      --build-arg OS_VERSION=${OS_VERSION}
      --build-arg OS_VERSION_NUMBER=${OS_VERSION_NUMBER}
      --build-arg OS_BUILD_NUMBER=${OS_BUILD_NUMBER}
      --build-arg PROJECT_COMPONENT=${RESTAPI_IMAGE}
      --build-arg PROJECT_VERSION=${PROJECT_VERSION}
      --build-arg PROJECT_PREFIX=${PROJECT_PREFIX}
  tags:
    - kaniko
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web" && $CI_COMMIT_BRANCH != "master"'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH != "master"'

build:restapi:latest:
  stage: build:image:lab
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  before_script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"${ARTIFACTORY_REPO}\":{\"auth\":\"${ARTIFACTORY_AUTH}\",\"email\":\"${ARTIFACTORY_EMAIL}\"}}}" > /kaniko/.docker/config.json
    - wget -qO - http://pki.mitre.org/MITRE%20BA%20ROOT.crt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
    - wget -qO - http://pki.mitre.org/MITRE%20BA%20NPE%20CA-3%281%29.crt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
    - wget -qO - http://pki.mitre.org/MITRE-chain.txt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
  script:
    - >-
      /kaniko/executor
      --cache=true
      --cache-repo=${ARTIFACTORY_UNTRUSTED_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}/cache
      --registry-mirror ${ARTIFACTORY_REPO}
      --context ${CI_PROJECT_DIR}
      --dockerfile ${CI_PROJECT_DIR}/docker/${RESTAPI_IMAGE}/Dockerfile
      --destination ${ARTIFACTORY_UNTRUSTED_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}:${CI_COMMIT_SHA}
      --destination ${ARTIFACTORY_UNTRUSTED_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}:latest
      --build-arg MINICONDA_VERSION=${MINICONDA_VERSION}
      --build-arg OS_VERSION=${OS_VERSION}
      --build-arg OS_VERSION_NUMBER=${OS_VERSION_NUMBER}
      --build-arg OS_BUILD_NUMBER=${OS_BUILD_NUMBER}
      --build-arg PROJECT_COMPONENT=${RESTAPI_IMAGE}
      --build-arg PROJECT_VERSION=${PROJECT_VERSION}
      --build-arg PROJECT_PREFIX=${PROJECT_PREFIX}
  tags:
    - kaniko
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web" && $CI_COMMIT_BRANCH == "master"'
    - if: '$CI_COMMIT_BRANCH == "master"'

build:restapi:tag:
  stage: build:image:lab
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  before_script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"${ARTIFACTORY_REPO}\":{\"auth\":\"${ARTIFACTORY_AUTH}\",\"email\":\"${ARTIFACTORY_EMAIL}\"}}}" > /kaniko/.docker/config.json
    - wget -qO - http://pki.mitre.org/MITRE%20BA%20ROOT.crt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
    - wget -qO - http://pki.mitre.org/MITRE%20BA%20NPE%20CA-3%281%29.crt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
    - wget -qO - http://pki.mitre.org/MITRE-chain.txt >> /kaniko/ssl/certs/additional-ca-cert-bundle.crt
  script:
    - >-
      /kaniko/executor
      --cache=true
      --cache-repo=${ARTIFACTORY_UNTRUSTED_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}/cache
      --registry-mirror ${ARTIFACTORY_REPO}
      --context ${CI_PROJECT_DIR}
      --dockerfile ${CI_PROJECT_DIR}/docker/${RESTAPI_IMAGE}/Dockerfile
      --destination ${ARTIFACTORY_REPO}/${PROJECT_PREFIX}/${RESTAPI_IMAGE}:${CI_COMMIT_TAG}
      --build-arg MINICONDA_VERSION=${MINICONDA_VERSION}
      --build-arg OS_VERSION=${OS_VERSION}
      --build-arg OS_VERSION_NUMBER=${OS_VERSION_NUMBER}
      --build-arg OS_BUILD_NUMBER=${OS_BUILD_NUMBER}
      --build-arg PROJECT_COMPONENT=${RESTAPI_IMAGE}
      --build-arg PROJECT_PREFIX=${PROJECT_PREFIX}
      --build-arg PROJECT_VERSION=${PROJECT_VERSION}
  tags:
    - kaniko
  rules:
    - if: "$CI_COMMIT_TAG"
